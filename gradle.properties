# Gradleのメモリ設定
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

# AndroidXを使用する設定
# Media3 / ExoPlayer は AndroidX ライブラリなので必須
android.useAndroidX=true

# 古いSupport Libraryが混ざった場合の変換設定
# 今回は必須ではありませんが、互換性のためONにしておきます
android.enableJetifier=true
